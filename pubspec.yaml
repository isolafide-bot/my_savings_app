name: my_savings_app
description: A savings app with edit and reset features.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0  # 이 줄이 반드시 있어야 에러가 나지 않습니다.

flutter:
  uses-material-design: true
