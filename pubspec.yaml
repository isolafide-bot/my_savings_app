name: my_savings_app
description: Advanced savings app with storage and stats.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  shared_preferences: ^2.2.2 # 데이터 저장용

flutter:
  uses-material-design: true
